# Kaamil Technology Sales — Environment Configuration
# Copy this file to .env and adjust values as needed.

# Flask settings
FLASK_PORT=5001
FLASK_DEBUG=false

# Data directory override (leave empty for default: ./data)
# TC_DATA_DIR=/path/to/data

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
